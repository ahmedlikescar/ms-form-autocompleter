# ms-form-autocompleter
as the name suggests, it auto completes Microsoft forms

u need selenium, python, firefox, and geckodriver

mac path assumed by default, change it if ur windows, otherwise it will crash on launch
